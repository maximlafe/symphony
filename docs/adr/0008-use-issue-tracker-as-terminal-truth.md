# Use the issue tracker as terminal truth

Symphony Runtime does not declare an Issue terminal from its own run outcome. Terminal completion belongs to the Issue Tracker's terminal Issue States, so dispatch, reconciliation, and cleanup decisions stay aligned with the tracker-visible workflow rather than a hidden internal success state.
