# Run unattended until classified handoff

An Issue Run is autonomous until Symphony Runtime or the Coding Agent reaches a classified Handoff. When required access, decisions, or actions are missing, the run records the reason and routes through the Handoff flow instead of asking a human for ad hoc follow-up inside the active execution loop.
