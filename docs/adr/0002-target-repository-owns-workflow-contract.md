# Target repository owns the workflow contract

The Target Repository owns the Workflow Contract that governs its Issue Runs. Keeping tracker scope, workspace bootstrap, agent operating rules, validation expectations, and Handoff requirements beside the code they affect lets changes to agent policy review, version, and roll back with the repository instead of becoming hidden global runtime configuration.
